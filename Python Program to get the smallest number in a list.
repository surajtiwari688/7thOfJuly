#Python program to get the smallest number from a list.

def smallest_num_in_list(list):
   min = list[0]

   for x in list:

     if x < min:
       min = x
   return min
print(smallest_num_in_list([27,9,88,3,4,43]))
